[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "certus-engine"
version = "0.1.0"
requires-python = ">=3.14.5"
dependencies = [
    "numpy>=2.4.6",
]

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "certus_engine"
